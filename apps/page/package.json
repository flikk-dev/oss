{
  "name": "@oss/page",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev --port=4721",
    "build": "bun run --cwd ../.. registry:build && next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  }
}
